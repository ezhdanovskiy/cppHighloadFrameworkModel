GET_FILENAME_COMPONENT(CURRENT_DIR ${CMAKE_CURRENT_LIST_FILE} PATH)

INCLUDE_DIRECTORIES(
	${CURRENT_DIR}/include
  ${CURRENT_DIR}/gtest/include
)

LINK_DIRECTORIES(
)

SET(GMOCK_LIBS gmock gmock_main )
